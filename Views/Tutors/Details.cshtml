@model s318344_Assignment1.Models.TutorModel

@{
    ViewData["Title"] = "Details - " + Model.TutorName;
}

<div class=" bg-primary p-4" style="border-top-left-radius: 2em; border-top-right-radius: 2em;">

    <h1 class="text-white">Details</h1>
    <h2 class="text-dark"> @Html.DisplayFor(model => model.TutorName)</h2>

    <hr />

    <a class="btn btn-warning" asp-action="Edit" asp-route-id="@Model?.TutorId">Edit</a>
    <a class="btn btn-danger" asp-action="Delete" asp-route-id="@Model?.TutorId">Delete</a>
    <a class="btn btn-outline-light" asp-action="Index">Back</a>

</div>

<div class="container-fluid p-3">
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.TutorName)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.TutorName)
        </dd>
    </dl>
</div>

